#ifndef EXACTO_H
#define EXACTO_H

#include <vector>
#include <set>
#include <algorithm>
#include <tuple>
#include "../common/common.h"

using namespace std;

pair<int,vector<int> > exacto(vector<nodo> &nodos, vector<int> &clique, unsigned pos);

#endif // EXACTO_H
